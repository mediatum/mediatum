//
  Copyright (C) since 2007, Technical University of Munich (TUM) and mediaTUM authors
  SPDX-License-Identifier: AGPL-3.0-or-later

#portal-breadcrumbs
  if logo and logo.getPath()
    #collectionlogo
      if logo.getURL()
        a(href=logo.getURL(), target='_blank')
          img(src=logo.getPath(), title=logo.getURL(), style='margin-left: 30px;')
      else
        img(src=logo.getPath(), style='margin-left: 30px;')
  div(style='text-align:right; float:right')
    p
      if styles|length > 1
        for style in styles
          a.view_switch(href=select_style_link(style=style.name), title=translate(style.label))
            img(src='/static/img/{}'.format(style.icon))
      if printlink
        a.view_switch(target='_blank', href=printlink, rel="nofollow", title=translate("printpdf"))
          img(src='/static/img/print_icon.png')
  if path
    div.mediatum-content-nav-path
      for step in path
        a.breadcrumb-delimiter(href=node_url(step.id))
          = step.getLabel()
      a(href=node_url(node.id))
        = node.getLabel()


// generated from jade
//- vim: set filetype=jade sw=2 ts=2 sts=2 expandtab:
