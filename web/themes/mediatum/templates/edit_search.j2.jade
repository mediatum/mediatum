//
  Copyright (C) since 2007, Technical University of Munich (TUM) and mediaTUM authors
  SPDX-License-Identifier: AGPL-3.0-or-later

#search-form.portlet
  if search.isSimple()
    span
      form#search-box(name='search', action=action)
        input#search-field(value=search.simple_search_query, placeholder=search_placeholder, name='query')
        input#search-button(value=translate('search'), title=translate('search_title'), type='submit')
        input(type='hidden', name='id', value=container_id)
        input(type='hidden', name='srcnodeid', value=container_id)
        if liststyle
          input(type='hidden', name='liststyle', value=liststyle)
